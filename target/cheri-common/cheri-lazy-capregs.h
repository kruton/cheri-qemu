    }
#ifdef CONFIG_DEBUG_TCG
    sanity_check_capreg(gpcrs, regnum);
#endif
    case CREG_INTEGER: {
        return result;
        sanity_check_capreg(gpcrs, regnum);
                                 const cap_register_t *newval)
    case CREG_INTEGER:
